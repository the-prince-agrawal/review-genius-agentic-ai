spring:
  application:
    name: review-genius-agent

agent:
  maxSteps: 5

github:
    #TODO : move these to env variables and use them here
    owner: princeagrawal024
    repo: log-intelligence-engine
    prNumber: 1
    token: ${LOG_INTELLIGENCE_GITHUB_ACCESS_TOKEN_FOR_PR_REVIEW}